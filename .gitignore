.DS_Store

# Stripe seeder OUTPUT — generated, legacy payment-link record. The seeder itself and its
# source-of-truth CSV ARE committed (they contain no secrets; the key is env-only).
scripts/stripe-links.json
node_modules/
package.json
package-lock.json
.dev.vars
.DS_Store
